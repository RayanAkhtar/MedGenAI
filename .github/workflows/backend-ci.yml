name: Backend CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  backend:
    runs-on: ubuntu-latest

    steps:
      # 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2: Set up Python
      - name: Set up Python 3.9
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      # 3: Install dependencies
      - name: Install dependencies
        working-directory: ./backend
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

      # 4: Run tests
      - name: Run backend tests
        working-directory: ./backend
        run: |
          source venv/bin/activate
          pytest

      # 5: Deploy to Heroku
      - name: Deploy to Heroku
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }} 
        run: |
          cd ./backend  
          git remote add heroku https://git.heroku.com/med-gen-ai.git
          git push heroku DevOps  
        # TODO change later to be main instead of devops
